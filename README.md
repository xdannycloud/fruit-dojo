# Cursor Agent Pet

A playful cursor-following coding-agent pet demo inspired by tiny AI toy demos.

Built through a local agent workflow: Claude Code implements, Codex reviews/fixes, tests must pass before GitHub push.
